/*
  Header occlusion is now a narrow CSS-only fade in header-occlusion.css.
  This file is intentionally inert and remains temporarily to avoid changing
  script ordering while the visual treatment is being evaluated.
*/
